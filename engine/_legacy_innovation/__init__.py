"""
Legacy Innovation Stack (Quarantined)
=====================================

These modules were generated as part of the original "Cheetah Marathon" build
but are truncated mid-file and contain unrecoverable syntax errors:

    - evolutionary_protocols.py   (SyntaxError: truncated signature)
    - cross_domain_fusion.py      (SyntaxError: truncated list comp)
    - innovation_engine.py        (SyntaxError: unterminated string)
    - meta_metaphor.py            (SyntaxError: unterminated string)
    - predictive_metaphors.py     (SyntaxError: unterminated docstring)
    - emotional_intelligence.py   (imports, but crashes on construction:
                                    undefined EmotionalState members)

They were quarantined here so the `engine` package imports cleanly.
Nothing in the active engine, UI, tests, or benchmarks references them.

To restore and repair: fix the truncations, then re-import from this package.
"""
