# Comic Book Metaphor Engine - Dependencies

# Core data processing
pandas>=2.0.0
numpy>=1.24.0

# Vector search and embeddings
faiss-cpu>=1.7.4
sentence-transformers>=2.2.0
scikit-learn>=1.3.0

# Web API (SaaS backend)
fastapi>=0.110.0
uvicorn>=0.29.0
pydantic>=2.5.0

# UI
gradio>=4.0.0

# Document ingestion (comic books, PDFs)
pdfplumber>=0.10.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-mock>=3.11.0
httpx>=0.27.0

# Utilities
python-dateutil>=2.8.2
tqdm>=4.65.0

# Optional document formats
rarfile>=4.0
py7zr>=0.20.0
Pillow>=10.0.0
ebooklib>=0.18.0

# Optional: OCR (needs tesseract binary installed separately)
# pytesseract>=0.3.10

# Optional: GPU acceleration (uncomment if available)
# faiss-gpu>=1.7.4

# SaaS: Supabase auth/storage/db + Stripe billing + Vercel
supabase>=2.5.0
stripe>=9.0.0
python-multipart>=0.0.9
a2wsgi>=1.10.0
